{
  "name": "doku",
  "productName": "Doku",
  "version": "0.1.5",
  "private": true,
  "icon": "apps/desktop/src/assets/icon.png",
  "description": "Doku — Your Second Mind",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.2.3"
  },
  "scripts": {
    "dev": "npm run dev --workspace doku-desktop",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:e2e": "npm run ensure:export-assets && npm run build --workspace doku-desktop && playwright test",
    "test:e2e:headed": "npm run ensure:export-assets && npm run build --workspace doku-desktop && playwright test --headed",
    "version:bump": "node scripts/version-bump.js",
    "version:show": "node scripts/version-bump.js --no-bump --dry-run",
    "verify:packaging": "node scripts/verify-packaging-assets.js",
    "test:packaging": "node --test tests/packaging/release-packaging.test.mjs",
    "dist": "npm run version:bump && npm run ensure:export-runtime && npm run ensure:deb-docs && npm run verify:packaging && npm run package:linux:all --workspace doku-desktop && npm run smoke:linux && npm run smoke:linux:rpm",
    "smoke:linux": "node scripts/smoke-linux-package.cjs",
    "smoke:linux:rpm": "node scripts/smoke-linux-rpm.cjs",
    "ensure:export-assets": "node scripts/ensure-export-assets.cjs",
    "ensure:deb-docs": "node scripts/ensure-deb-docs.cjs",
    "ensure:weasy-runtime": "node scripts/ensure-weasy-runtime.cjs",
    "ensure:latex-runtime": "node scripts/ensure-latex-runtime.cjs",
    "ensure:bundle-native-runtime": "node scripts/bundle-linux-native-runtime.cjs",
    "ensure:export-runtime": "npm run ensure:weasy-runtime && npm run ensure:latex-runtime && npm run ensure:bundle-native-runtime",
    "package:dir": "npm run ensure:export-assets && npm run package:dir --workspace doku-desktop",
    "package:linux": "npm run ensure:export-runtime && npm run package:linux --workspace doku-desktop && npm run smoke:linux",
    "package:linux:deb": "npm run ensure:export-runtime && npm run ensure:deb-docs && npm run verify:packaging && npm run package:linux:deb --workspace doku-desktop",
    "package:linux:rpm": "npm run ensure:export-runtime && npm run package:linux:rpm --workspace doku-desktop && npm run smoke:linux:rpm",
    "package:mac": "npm run ensure:export-assets && npm run package:mac --workspace doku-desktop",
    "package:win": "npm run ensure:export-runtime && npm run package:win --workspace doku-desktop",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write ."
  },
  "overrides": {
    "@electron/get": {
      "undici": "^7.29.0"
    },
    "jsdom": {
      "undici": "^7.29.0"
    }
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.73",
    "@types/react-dom": "^18.2.23",
    "@typescript-eslint/eslint-plugin": "^7.4.0",
    "@typescript-eslint/parser": "^7.4.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.34.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "jsdom": "^29.0.2",
    "prettier": "^3.2.5",
    "typescript": "^5.4.3",
    "vitest": "^4.1.5"
  }
}
